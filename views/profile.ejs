<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Sign In</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <link
      href="https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/css/profile.css" />
  </head>
  <body>
    <% if (alerts.success) { %> <% alerts.success.forEach(msg => { %>

    <div class="alert alert-success"><%= msg %></div>
    <% }) %> <% } %> <% if (alerts.error) { %> <% alerts.error.forEach(msg => {
    %>
    <div class="alert alert-danger"><%= msg %></div>
    <% }) %> <% } %>

    <header>
      <nav class="navbar">
        <a class="navbar-brand" href="/">DailyPoints</a>
        <ul>
          <% if(!currentUser) { %>
          <li><a href="/auth/signup">Signup</a></li>
          <li><a href="/auth/login">Login</a></li>
          <% } else { %>
          <li><a href="/auth/logout">Logout</a></li>
          <li><a href="/profile">Profile</a></li>
          <% } %>
        </ul>
      </nav>
    </header>

    <div class="container text-center profile">
      <div class="row">
    <div class="col">
      <h2 class="heading">Update <span>Products</span></h2>
      <div class="home-img">
        <img src="https://i.imgur.com/QkIa5tT.jpeg" alt="" />
        <a href="/daily" class="btn">Help!</a>
      </div>
      <h2 class="heading">Review <span>Products</span></h2>
      <div class="home-img">
        <img src="https://i.imgur.com/QkIa5tT.jpeg" alt="" />
        <a href="/daily" class="btn">Review!</a>
      </div>
    </div>
    <div class="col">
      <h2 class="heading">Profile <span>Stats</span></h2>
      <div id="profile">
        
      <div class="input-box">
        <i class='bx bx-info-circle' ></i>
          <h3>Name: <span><%= name %>!</h3>
          <h3>Email: <span><%= email %>! </h3>
      </div>
      <div class="input-box">
        <i class="bx bx-coin"></i>
        <h3>Points Earned: <span><%= name %>!</h3>
        <h3>Streak: <span><%= email %>! </h3>
      </div>
      <div class="input-box">
        <i class='bx bx-bar-chart-alt-2' ></i>
        <h3>LeaderBoard: <span><%= name %>!</h3>
        <h3>Top 3 reviewers: <span><%= name %>! </h3>
      </div>
      <div class="input-box">
        <i class='bx bx-edit' ></i>
        <h3>Total Reviews: <span><%= name %>!</h3>
        <h3>Most Recent Review: <span><%= email %>! </h3>
         <a href="/fruits/edit" class="btn">Edit</a>
          <a href="/fruits/delete" class="btn">Delete</a>
        </div>
      <div class="input-box">
        <i class='bx bx-edit' ></i>
        <h3>Total Products: <span><%= name %>!</h3>
        <h3>Most Recent Product: <span><%= email %>! </h3>
         <a href="/fruits/edit" class="btn">Edit</a>
          <a href="/fruits/delete" class="btn">Delete</a>
        </div>
      </div>
    </div>
    </div>
  </body>
</html>
